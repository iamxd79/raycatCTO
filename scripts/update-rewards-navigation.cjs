const fs=require('fs');let p='app/page.tsx',s=fs.readFileSync(p,'utf8');s="import SiteNav from '../components/SiteNav';\nimport RewardSteps from '../components/RewardSteps';\n"+s;s=s.replace(/function Nav\(\)\{[\s\S]*?\}\r?\nexport default/,'export default');s=s.replace('<Nav/>','<SiteNav/>');s=s.replace(/<section className="rewards section"[\s\S]*?<\/section>/,'<RewardSteps/>');s=s.replace('<section className="final section">','<section className="final section" id="buy">');fs.writeFileSync(p,s);p='components/dashboard/Dashboard.tsx';s=fs.readFileSync(p,'utf8').replace('<section className="live section raycat-terminal"','<section id="live" className="live section raycat-terminal"');fs.writeFileSync(p,s);
