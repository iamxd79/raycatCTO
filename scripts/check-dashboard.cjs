const {chromium}=require('@playwright/test');
async function main(){const browser=await chromium.launch({channel:'chrome',headless:true});for(const width of [1440,768,390]){const page=await browser.newPage({viewport:{width,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://localhost:3000/',{waitUntil:'networkidle'});const terminal=page.locator('.raycat-terminal');await terminal.scrollIntoViewIfNeeded();await terminal.screenshot({path:`review/dashboard-${width}.jpg`,type:'jpeg',quality:80});if(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth))throw Error('Overflow '+width);if(errors.length)throw Error(errors.join('\n'));console.log(width+': no overflow/runtime errors');await page.close();}await browser.close();}
main().catch(e=>{console.error(e);process.exit(1)});
