const {chromium}=require('@playwright/test');
async function main(){const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage({reducedMotion:'reduce',viewport:{width:390,height:844}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost:3000/');await p.getByRole('link',{name:'Enter the RayCat Museum',exact:true}).click();await p.locator('.phase-inside').waitFor({timeout:20000});await p.getByRole('button',{name:'GUIDE: ON'}).click();await p.getByRole('button',{name:'GUIDE: OFF'}).waitFor();await p.goto('http://localhost:3000/museum?room=old-world&fallback=1');await p.locator('.flat-art').first().focus();await p.getByText('They called it rent.',{exact:false}).first().waitFor();if(errors.length)throw Error(errors.join('\n'));await b.close();console.log('One-click automatic entry, guide toggle, room deep link and fallback dialogue passed.');}
main().catch(e=>{console.error(e);process.exit(1)});
