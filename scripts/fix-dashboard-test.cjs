const fs=require('fs');const p='scripts/check-dashboard-failures.cjs';let s=fs.readFileSync(p,'utf8');s=s.replace('await page.clock.fastForward(91000);await page.waitForTimeout(1500);',"const refreshed=page.waitForResponse(r=>r.url().endsWith('/api/dashboard')&&r.status()===200);await page.clock.fastForward(91000);await refreshed;await page.waitForTimeout(300);");fs.writeFileSync(p,s);
